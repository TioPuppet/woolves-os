# Supabase — fill with real values before M1 (auth).
# These are PLACEHOLDERS for M0; the app scaffolds without a live connection.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-only (used from Edge Functions in M8; never expose to client).
# ANTHROPIC_API_KEY=sk-ant-...
